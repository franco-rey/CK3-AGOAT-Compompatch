# AGOAT: AGOT - Unbound checks `NOT = { has_character_modifier = au_dragonguard_cooldown_5y }` in two of its guard interactions but
# never defines (or adds) that modifier, so the trigger logs an error on every evaluation and resolves as false. Defining it makes the
# check valid; behaviour is unchanged because nothing applies the modifier.
au_dragonguard_cooldown_5y = {
	icon = prowess_negative
}
