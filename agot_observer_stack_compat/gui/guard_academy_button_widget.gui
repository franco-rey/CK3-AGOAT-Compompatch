widget = {
	name = "guard_academy_button_widget"
	parentanchor = bottom|right
	widgetanchor = bottom|right
	# Standalone layout occupies the normal bottom HUD row.
	# Observer Stack P47 - HUD collision with Search & Trade Artifacts and
	# Advanced Character Search.
	#
	# All three mods anchor bottom|right, so a position of -X places the widget's
	# right edge X px from the screen edge and it extends LEFTWARD by its width.
	# Measured leftward from the right edge, the row was:
	#     ACS  'find person'      630 -> 685   (55 wide, acs_external_integration.gui)
	#     THIS widget             631 -> 745   (114 wide)   <-- covered both
	#     S&T  'find artifact' R  690 -> 746   (56 wide, at_find_artifacts_widget.gui)
	#     S&T  'find artifact' C  780 -> 836
	#     S&T  'find artifact' L  870 -> 926
	# At -631 this widget sat on top of 54 of ACS's 55 px and 55 of S&T-right's 56,
	# hiding both buttons behind the guard row.
	#
	# Moved to -960, which continues S&T's own 90 px pitch (870 + 90) and leaves a
	# 34 px gap after S&T-left ends at 926 -- the same gap S&T uses between its own
	# buttons. New span 960 -> 1074. Nothing else in the playset anchors bottom|right
	# beyond 926, and vanilla's timeline_widget occupies only 0 -> 649, so this is
	# clear space. Size and all three button bindings are unchanged.
	position = { -960 0 }
	size = { 114 36 }
	# Observer Stack P36 - same defect P24 fixed for the Grand Remembrance chronicle
# window. And() does not short-circuit, so IsShown(GetPlayer.MakeScope) was
# evaluated even when GetPlayer.IsValid was false, handing the scripted GUI a
# dangling character reference every frame. 4,542 errors in one session. A
# script-side guard cannot catch it -- the scope is typed Character and simply
# points at nothing -- so the binding itself is gated on GetPlayer.IsValid first.
	visible = "[And(GetPlayer.IsValid, And(And(Not(IsPauseMenuShown), IsDefaultGUIMode), GetScriptedGui('agot_guard_positions_standalone_hud_layout').IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End)))]"
	alwaystransparent = no
	layer = hud_layer

	flowcontainer = {
		ignoreinvisible = yes
		parentanchor = bottom|right
		button_round = {
			visible = "[And(And(GetScriptedGui('agot_guard_positions_view_guard_roster_button').IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End), Not(IsGameViewOpen('barbershop'))), IsDefaultGUIMode)]"
			name = "agot_guard_roster_button"
			datacontext = "[GetScriptedGui( 'agot_guard_positions_view_guard_roster_button' )]"
			onclick = "[ScriptedGui.Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).End)]"
			tooltip = "AGOT_GUARD_POSITIONS_VIEW_GUARD_ROSTER"
			button_icon_highlight = {
				texture = "gfx/interface/icons/flat_icons/kingsguard_flat_black.dds"
				parentanchor = center
				alwaystransparent = yes
				size = { 36 36 }
			}
		}
		button_round = {
			visible = "[And(And(GetScriptedGui('agot_guard_positions_view_guard_academy_button').IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End), Not(IsGameViewOpen('barbershop'))), IsDefaultGUIMode)]"
			name = "agot_guard_academy_button"
			datacontext = "[GetScriptedGui( 'agot_guard_positions_view_guard_academy_button' )]"
			onclick = "[ScriptedGui.Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).End)]"
			tooltip = "AGOT_GUARD_POSITIONS_VIEW_GUARD_ACADEMY"
			button_icon_highlight = {
				texture = "gfx/interface/icons/domicile_building/domicile_building_training_circle.dds"
				parentanchor = center
				alwaystransparent = yes
				size = { 25 25 }
				alpha = 0.78
			}
		}
		button_round = {
			visible = "[And(And(GetScriptedGui('agot_guard_positions_view_mission_ledger_button').IsShown(GuiScope.SetRoot(GetPlayer.MakeScope).End), Not(IsGameViewOpen('barbershop'))), IsDefaultGUIMode)]"
			name = "agot_guard_mission_ledger_button"
			datacontext = "[GetScriptedGui( 'agot_guard_positions_view_mission_ledger_button' )]"
			onclick = "[ScriptedGui.Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).End)]"
			tooltip = "AGOT_GUARD_POSITIONS_VIEW_MISSION_LEDGER"
			button_icon_highlight = {
				texture = "gfx/interface/icons/artifact/artifact_chronicle.dds"
				parentanchor = center
				alwaystransparent = yes
				size = { 25 25 }
				alpha = 0.52
			}
		}
	}
}
